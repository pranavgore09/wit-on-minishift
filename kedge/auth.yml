name: auth
containers:
- image: docker.io/fabric8/fabric8-auth:latest
  env:
  - name: AUTH_WIT_URL
    value: "http://core:8080"
  - name: AUTH_DEVELOPER_MODE_ENABLED
    value: "true"
services:
- name: auth
  ports:
  - port: 8089
